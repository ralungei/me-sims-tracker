import Foundation

/// Copy this file to `BackendCredentials.swift` and fill in your own values.
/// `BackendCredentials.swift` is gitignored.
enum BackendCredentials {
    static let baseURL = URL(string: "https://your-worker.workers.dev")!
    static let apiKey  = "REPLACE_WITH_YOUR_API_KEY"
}
